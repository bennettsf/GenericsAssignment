/**
 * Utility class used to pull info from Library media.
 *
 * @author Bennett Fife
 * @version 11/2/2023
 */
public class NSCMediaUtils {
    /**
     * static method used to print the data of a generic object
     * @param mediaObject generic that we want to show data of
     * @param <T> generic type
     */
    static <T> void printMediaDetails(T mediaObject){
        System.out.println(mediaObject.toString());

    }
}
